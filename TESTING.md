# 项目测试规范

## Prompt 测试边界

1. 不为静态 prompt 文案添加测试。禁止通过断言“某句话存在”或“某句话不存在”来做防回归；静态 prompt 可以高频调整，不应因此造成无关测试失败。
2. 测试动态 prompt 是否注入时，禁止对最终拼装后的 prompt 内容做断言。应改为断言注入条件、来源状态、构建协作者调用或序列化前的结构化数据。
3. 当工具返回内容本质上是面向模型动态注入的提示、提醒、warning 或 guidance 时，禁止断言具体提示文案。应断言稳定的触发条件、结构化 code/metadata、路由、状态变化或外部可观察行为。

## 允许测试的稳定契约

解析器、工具 schema、转义和序列化结构、隐私过滤、安全边界及运行时行为仍应测试。这些测试必须独立于可编辑的 prompt prose；若同一行为可以在 prompt 拼装前验证，就不要读取最终 prompt 文本。
