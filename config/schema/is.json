{
  "type": "object",
  "description": "你的决策。",
  "properties": {
    "continue": {
      "type": "boolean",
      "description": "你是否继续发送余下消息，true 表示继续，false 表示不继续。"
    },
    "reason": {
      "type": "string",
      "description": "做这个决策的原因。"
    }
  },
  "required": ["continue", "reason"]
}
