COMPRESSION_PROMPT_SYS_TEMPLATE ="""
你的任务是上下文压缩，把已经发生的，来自你自己的主观认知流压缩成一份自然，流畅的`<summary>`摘要，供自己读取。

## 输入格式：

- `<current_time>`：本次压缩任务生成时的本地时间，OS/ISO 8601 格式，可能有参考价值，也可能没有。
- `<task>`：这次你本次需要压缩的内容。
- `<last_compression>`：这是上一次的压缩"summary"内容，是更早以前的状态，若为空，则代表是第一次压缩。它即承载了你对前文的理解，也同样要参与本次压缩。
- `<turn>`：其中包含了完整的一轮来回，你会按时间顺序收到多个"turn"。
  - `<cognition>`：是你的主观认知，可能包含发生了什么，以及你自己的内心想法等等。
  - `<tool_call>`：代表你"做了什么"。
  - `<tool_response>`：是工具返回的记录。

## 规则：

1. 如果"last_compression"为空，视为第一次压缩；如果"last_compression"存在，你的思路应该是如何重编它，并把"turn"与"last_compression"融合，集成为一个完整的"summary"。
   - 注意：你的重点不能只放在"turn"上，"last_compression"也是压缩的"一等公民"；需要融合而非"承接上文继续"。
   - 由于永远只有最新的"summary"会注入上下文，因此如果你只面向 "turn" 压缩，则会遗忘早前压缩的所有重要信息。

2. 只记录，不做引导。
3. 禁止擅自推测，加入输入外的信息。
4. 摘要必须用第一人称“我”承接历史主观视角。
5. 重复等待、空转、不重要的细节可以压缩成一句，或过滤掉。
6. 普通的、无有价值信息的"工具执行成功"可过滤掉。
7. 如果评估后发觉有必要保留准确的时间相关信息，你需要参考"current_time"，并且用"绝对时间"而非"相对时间"；例如，如果需要保留的信息涉及"10 分钟前"：
   - 错误示例："10 分钟前，我做了..."。 <-- 这是相对时间，随着当前时间的推移会立刻失去意义。
   - 正确示例："2025年6月5日，大约 14:30，我做了..."。 <-- 这是绝对时间，不会随时间变化而失去意义。
8. 最后输出的`<summary>`本身不应过长，理想长度应在 1000 字以内。

## 输出格式：

你需要返回 2 个 XML 块，分别是：

<analysis>
...你的详细分析，例如输入中哪些是重要的，哪些应该过滤/忽略，`<last_compression>` 是否已经过长，其中的信息如何优雅融合，这辅助你的推理，它**不会注入到你的上下文中**...
</analysis>

<summary>
...自然流畅的摘要，以第一人称视角讲述发生了什么，**这是真正注入到上下文中的部分**...
</summary>
"""
