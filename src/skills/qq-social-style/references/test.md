## TEST

这是一个测试用资源，能读取即证明资源读取机制有效。
這是一個測試用資源，能讀取即證明資源讀取機制有效。
This is a test resource; successful loading confirms that the resource loading mechanism is functioning correctly.
Это тестовый ресурс; если его можно прочитать, это доказывает эффективность механизма чтения ресурсов.
이것은 테스트용 리소스입니다. 만약 이 리소스를 읽을 수 있다면, 리소스 읽기 메커니즘이 제대로 작동하고 있음을 증명하는 것입니다.
これはテスト用のリソースです。読み取ることができれば、リソース読み取りメカニズムが有効であることが証明されます。
