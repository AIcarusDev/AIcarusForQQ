DESCRIPTION = """
向当前打开的会话窗口发送一条或多条消息。
用法：
  - 数组中的每一个 "segments" 代表一条独立发送的消息。
  - 支持一次调用发送多条消息（即多个 "segments" ），会按顺序依次发送。
  - 支持单个 "segments" 中拼接不同的内容，将会合并为单条消息发送。

建议：
  - 将长消息拆分为多条短消息发送。
  - 每条消息可以非常简短（甚至 5 个字以下），且不需要严谨的标点符号（例如句号结尾）。
"""