modules:
  core:
    path: core
    always_active: true
    namespaces:
      - core

  browser:
    path: browser
    active_when: browser_available
    namespaces:
      - browser_use
      - browser_runtime
    mounts: []

